name: data_validator
description: A validation for all kinds of data.
version: 1.0.0
author: Nishant Subedi
homepage: https://github.com/nishantsubedi/data_validator

environment:
  sdk: ">=2.0.0-dev.68.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

 